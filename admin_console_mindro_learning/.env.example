# Base URL of the NestJS API (no trailing slash)
REACT_APP_API_BASE=http://localhost:3000

# Static bearer token for admin API access
REACT_APP_API_BEARER_TOKEN=your-token-here

# Port for the dev server (optional, defaults to 3000)
PORT=3001
